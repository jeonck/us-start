# Docusaurus 상단 메뉴별 사이드바 분리 가이드

## 목표

상단 메뉴(navbar) 클릭 시 해당 메뉴의 페이지만 사이드바에 표시되도록 구성한다.
참조 레포: [jeonck/us-work-english](https://github.com/jeonck/us-work-english)

---

## 핵심 패턴

Docusaurus의 `type: 'docSidebar'` navbar 항목과 카테고리별 사이드바를 연결하면,
메뉴 클릭 시 자동으로 해당 사이드바만 좌측에 표시된다.

```
navbar item (type: 'docSidebar', sidebarId: 'englishSidebar')
        │
        └──▶ sidebars.ts의 englishSidebar
                  └──▶ 해당 카테고리 docs만 표시
```

---

## 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `sidebars.ts` | 단일 autogenerated → 카테고리별 명시적 사이드바 |
| `docusaurus.config.ts` | navbar `to:` 링크 → `type: 'docSidebar'` |

---

## 1. sidebars.ts

### 이전 (잘못된 방식)

```ts
// 전체 docs를 하나의 사이드바로 자동 생성 → 모든 페이지에서 전체 목록 표시
const sidebars: SidebarsConfig = {
  tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],
};
```

### 이후 (올바른 방식)

카테고리마다 별도 사이드바를 정의하고, `items`에 해당 카테고리의 doc ID를 명시한다.

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  englishSidebar: [
    {
      type: 'category',
      label: '🗣️ 영어',
      collapsed: false,
      items: [
        'english/speaking',
        'english/phrasal-verbs',
      ],
    },
  ],

  housingSidebar: [
    {
      type: 'category',
      label: '🏠 주거',
      collapsed: false,
      items: ['housing/overview'],
    },
  ],

  financeSidebar: [
    {
      type: 'category',
      label: '💰 금융',
      collapsed: false,
      items: [
        'finance/overview',
        'finance/overseas-asset-transfer',
        'finance/robinhood',
      ],
    },
  ],

  leisureSidebar: [
    {
      type: 'category',
      label: '🏌️ 골프',
      collapsed: false,
      items: [
        'leisure/golf/overview',
        'leisure/golf/gear',
        'leisure/golf/membership',
        'leisure/golf/tips',
      ],
    },
  ],

  // 나머지 카테고리도 동일한 패턴으로 추가
};

export default sidebars;
```

**규칙**
- `sidebarId` 이름은 자유롭게 지정 (단, `docusaurus.config.ts`의 `sidebarId`와 일치해야 함)
- `items`의 doc ID는 `docs/` 폴더 기준 상대 경로에서 `.md` 확장자를 제외한 값
- `collapsed: false` → 사이드바 진입 시 카테고리가 펼쳐진 상태로 표시

---

## 2. docusaurus.config.ts — navbar

### 이전 (잘못된 방식)

```ts
// 단순 URL 링크 → 사이드바 자동 연결 없음
{to: '/docs/english/speaking', label: '🗣️ 영어', position: 'left'},
{to: '/docs/finance/overview', label: '💰 금융', position: 'left'},
```

### 이후 (올바른 방식)

```ts
// sidebarId로 사이드바와 직접 연결
{type: 'docSidebar', sidebarId: 'englishSidebar',   position: 'left', label: '🗣️ 영어'},
{type: 'docSidebar', sidebarId: 'housingSidebar',   position: 'left', label: '🏠 주거'},
{type: 'docSidebar', sidebarId: 'adminSidebar',     position: 'left', label: '📋 행정'},
{type: 'docSidebar', sidebarId: 'educationSidebar', position: 'left', label: '🎓 교육'},
{type: 'docSidebar', sidebarId: 'financeSidebar',   position: 'left', label: '💰 금융'},
{type: 'docSidebar', sidebarId: 'leisureSidebar',   position: 'left', label: '🏌️ 골프'},
{type: 'docSidebar', sidebarId: 'lifestyleSidebar', position: 'left', label: '🍎 맛집/생활'},
```

**`type: 'docSidebar'` 동작 방식**
- 클릭 시 해당 사이드바의 **첫 번째 doc**으로 자동 이동
- 좌측 사이드바에는 해당 `sidebarId`에 속한 항목만 표시
- 별도 frontmatter(`displayed_sidebar`) 없이 Docusaurus가 자동으로 매핑

---

## 주의: 피해야 할 방식 — `displayed_sidebar` frontmatter

하나의 통합 사이드바(`mainSidebar`)를 만들고 개별 파일에 `displayed_sidebar`를 붙이는 방법은 **동작하지만 권장하지 않는다**.

```yaml
# ❌ 권장하지 않음 — 파일마다 frontmatter 추가 필요
---
title: 로빈후드 가입과 운영
displayed_sidebar: mainSidebar
---
```

**문제점**
- 새 파일 추가 시마다 frontmatter를 직접 설정해야 함
- 사이드바 구조 변경 시 모든 파일을 수정해야 함
- 사이드바가 메뉴별로 분리되지 않아 UX 혼란

---

## 새 메뉴/카테고리 추가 절차

1. `docs/` 하위에 카테고리 폴더 및 `.md` 파일 생성
2. `sidebars.ts`에 새 사이드바 항목 추가
3. `docusaurus.config.ts` navbar에 `type: 'docSidebar'` 항목 추가

```ts
// sidebars.ts
healthSidebar: [
  {
    type: 'category',
    label: '🏥 의료',
    collapsed: false,
    items: [
      'health/overview',
      'health/insurance',
    ],
  },
],

// docusaurus.config.ts navbar
{type: 'docSidebar', sidebarId: 'healthSidebar', position: 'left', label: '🏥 의료'},
```

---

## 별도 plugin의 사이드바 (docs-ai)

`docs-ai` 처럼 별도 `plugin-content-docs` 인스턴스를 사용하는 경우,
해당 플러그인의 `sidebars-ai.ts`에 동일한 패턴을 적용한다.
navbar에서는 `type: 'docSidebar'`가 아닌 드롭다운(`type: 'dropdown'`)으로 연결할 수 있다.
